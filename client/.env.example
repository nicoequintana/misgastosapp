# ==================== CLIENTE ====================
# Las variables VITE_* quedan embebidas en el bundle JS y son publicas.
# Usar SOLO claves anon/public de Supabase. NUNCA la service role key.

VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_key_aqui

# URL del backend (dejar vacio en produccion si se sirve desde el mismo origen)
VITE_BACKEND_URL=http://localhost:3001
